name: sofia
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^11.0.0
  go_router: ^17.5.0
  http: ^1.6.0
  provider: ^6.1.5+1
  syncfusion_flutter_calendar: ^34.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
